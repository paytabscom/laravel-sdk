includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 8
    paths:
        - src
        - config
        - routes
    # Package config ships env() defaults, which is the documented Laravel convention.
    configDirectories:
        - config